<!DOCTYPE html>
<html lang = "en">
<head>
    <meta charset = "UTF-8">
    <meta http-equiv = "X-UA-Compatible" content = "IE=edge">
    <meta name = "viewport" content = "width=device-width, initial-scale=1.0">
    <title>iWorld</title>
    <link rel = "stylesheet" href = "css/Style.css">
    <link rel = "stylesheet" href = "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css">
</head>
<body>
    <div class = "container">
        <section class = "section-4">
        </section>
    </div>
    <script>
        async function loadiPhone13Page()
        {
            let request = new Request("elements.json")
            let response = await fetch(request)
            let json_obj = await response.json()

            const C = new iPhone13(json_obj);
            C.Load();    
        }

        class iPhone13
        {
            constructor(json_obj)
            {
                this.json_obj = json_obj;
            }

            Load()
            {
                const {json_obj} = this;
                this.loadLogo(json_obj);
                this.loadNavBar(json_obj);
                this.loadImage(json_obj);
            }
        
            loadLogo(P_json_obj)
            {
                let s4 = document.querySelector(".section-4")
                
                let a = document.createElement("a")
                a.href = "/"
                a.className = "logo"

                let i = document.createElement("i")
                i.className = "fa-brands fa-apple"

                a.appendChild(i)

                s4.appendChild(a)
            }

            loadNavBar(P_json_obj)
            {
                let s4 = document.querySelector(".section-4")
                let nav = P_json_obj['av1']
                
                let navbar = document.createElement("nav")
                navbar.className = "navbar"

                for(let i of nav)
                {
                    let navbar_link = document.createElement("a")
                    navbar_link.className = "navbar-link"

                    navbar_link.innerHTML = i.link
                    navbar_link.href = i.href

                    navbar.appendChild(navbar_link)
                    
                }
                s4.appendChild(navbar)
            }

            loadImage(P_json_obj)
            {
                let s4 = document.querySelector(".section-4")
                let imga = P_json_obj['Imga']

                let todo = document.createElement("div")
                todo.className = "todo" 

                for (let i of imga)
                {
                    let imgcontain = document.createElement("div")
                    imgcontain.className = "img-container"
                    imgcontain.style.textAlign = "center"

                    let img = document.createElement("img")
                    img.src = i.img1
                    img.style.width = "100%"
                    
                    let span = document.createElement("span")
                    span.style.fontSize = "25px"
                    span.innerHTML = i.span

                    imgcontain.appendChild(img)
                    imgcontain.appendChild(span)

                    todo.appendChild(imgcontain)
                }

                let imgcontain = document.createElement("div")
                imgcontain.className = "img-container"
                imgcontain.style.textAlign = "center"

                let img = document.createElement("img")
                img.src = P_json_obj['img4']
                img.style.width = "85%"
                img.style.paddingBottom = "12px"
                img.style.paddingTop = "75px"

                let span = document.createElement("span")
                span.style.fontSize = "25px"
                span.innerHTML = P_json_obj['span4']

                imgcontain.appendChild(img)
                imgcontain.appendChild(span)

                todo.appendChild(imgcontain)
                s4.appendChild(todo)
            }
        }

        loadiPhone13Page()
    </script>
</body>
</html>
